#include "ev_socket.hpp"

class tls_server_connection : public ev_socket
{
private:
    /* data */
public:
    tls_server_connection(/* args */);
    ~tls_server_connection();
};

tls_server_connection::tls_server_connection(/* args */)
{
}

tls_server_connection::~tls_server_connection()
{
}
