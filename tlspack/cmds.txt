traffic_start(){
    sudo docker run --name "$1_0" --rm -it -d --volume=/root/rundir:/rundir tgen /rundir/bin/tlspack.exe start "$1" "$2" /root/rundir
}

traffic_stop(){
    sudo docker run --rm -it --volume=/root/rundir:/rundir tgen /rundir/bin/tlspack.exe stop "$1"
}

traffic_stats(){
}
